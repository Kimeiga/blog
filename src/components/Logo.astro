---
import { readFile } from 'node:fs/promises';
import { resolve } from 'node:path';

const logo = await readFile(resolve(process.cwd(), 'public/kimeiga-white.svg'), 'utf8');
---
<Fragment set:html={logo} />
